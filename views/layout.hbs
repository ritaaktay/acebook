<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>
    {{!-- <link rel="stylesheet" href="/stylesheets/style.css" /> --}}
    {{!-- Latest compiled and minified CSS from Bootstrap --}}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
    {{!-- Latest compiled JavaScript from Bootstrap --}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    
    <nav class="navbar navbar-expand-md bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" id="home-button" href="/posts">Meowbook</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          {{#if session.user}}
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/posts/new">Make a post</a>
              </li>
              <li class="nav-item">
                <a id="settings-navbar" class="nav-link active" aria-current="page" href="/settings">Settings</a>
              </li>
              <li class="nav-item">
                <a id="settings-navbar" class="nav-link active" aria-current="page" href="/users/{{session.user._id}}">Profile</a>
              </li>

              <form action="/sessions?_method=DELETE" method="POST">
                <input class="btn btn-primary" id="logout" type="submit" value="Log Out" />
              </form>
            </ul>

          {{else}}
            
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/sessions/new">Log in</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/users/new">Sign up</a>
              </li>
            </ul>
          {{/if}}
        </div>
      </div>
    </nav>
    {{{body}}}
  </body>
</html>